[package]
name = "sky-trie"
version.workspace = true
edition.workspace = true

[dependencies]
sky-types.workspace = true

[dev-dependencies]
anyhow.workspace = true
tempfile.workspace = true
tokio = { workspace = true, features = ["macros", "fs"] }
sky-types = { workspace = true, features = ["fs"] }
